#incluse <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    /* Use puts to print a string followed by a new line */
    puts("Programming is like building a multilingual puzzle");

    /* Return 0 to indicate success */
    return (0);
}
