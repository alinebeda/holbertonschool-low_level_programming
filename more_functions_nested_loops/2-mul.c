#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers and returns the result
 * @a: The first integer to multiplie
 * @b: The second integer to multiplie
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
